#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int n,a,flag=0,i;
    scanf("%d",&n);
    a=n;
    int j=sqrt(a);
    if(n>=1 && n<pow(10,5)){
    if(n<2){
        printf("no");
    }
    else{
    for(i=2;i<j;i++){
        if(n%i==0){
            flag=1;
        }
    }
    if(flag!=1)
        printf("yes");
    else
        printf("no");
    }
    }
    return 0;
}